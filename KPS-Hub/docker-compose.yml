version: '3.8'

services:
  kps-hub:
    build: .
    container_name: kps-hub-server
    restart: always
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=8000
