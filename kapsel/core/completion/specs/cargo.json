{
  "name": "cargo",
  "description": "Rust's package manager for downloading dependencies and compiling packages",
  "options": [
    { "name": ["-V", "--version"], "description": "Print version info and exit" },
    { "name": ["-h", "--help"], "description": "Print help information" },
    { "name": ["-q", "--quiet"], "description": "Do not print cargo log messages" },
    { "name": ["-v", "--verbose"], "description": "Use verbose output" }
  ],
  "subcommands": [
    {
      "name": "build",
      "description": "Compile the current package",
      "options": [
        { "name": ["-r", "--release"], "description": "Build artifacts in release mode, with optimizations" },
        { "name": ["--target"], "description": "Build for the target triple" },
        { "name": ["--bin"], "description": "Build only the specified binary" }
      ]
    },
    {
      "name": "run",
      "description": "Run a binary or example of the local package",
      "options": [
        { "name": ["-r", "--release"], "description": "Build artifacts in release mode, with optimizations" },
        { "name": ["--example"], "description": "Run the specified example" },
        { "name": ["--bin"], "description": "Run the specified binary" }
      ]
    },
    {
      "name": "test",
      "description": "Execute all unit and integration tests and build examples of a local package",
      "options": [
        { "name": ["--no-run"], "description": "Compile, but don't run tests" }
      ]
    },
    {
      "name": "check",
      "description": "Check a local package and all of its dependencies for errors",
      "options": [
        { "name": ["--all-targets"], "description": "Check all targets" }
      ]
    },
    {
      "name": "add",
      "description": "Add dependencies to a Cargo.toml manifest file",
      "options": [
        { "name": ["-F", "--features"], "description": "Space or comma separated list of features to activate" },
        { "name": ["--dev"], "description": "Add as development dependency" }
      ]
    },
    {
      "name": "update",
      "description": "Update dependencies as recorded in the local lock file"
    },
    {
      "name": "new",
      "description": "Create a new cargo package at <path>",
      "options": [
        { "name": ["--bin"], "description": "Use a binary (application) template [default]" },
        { "name": ["--lib"], "description": "Use a library template" }
      ]
    }
  ]
}
